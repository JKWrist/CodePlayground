#include <iostream>
#include <string>
#include <stdio.h>

using namespace std;
const int L = 11000;
string mul(string a, string b)
{
	string s;
	int na[L], nb[L], nc[L], La = a.size(), Lb = b.size(), i, j;
	
}
int main(int argc, char *argv[]) 
{
	fill(na, na + L, 0);
	fill(na, na)
}